---
// No server-side data needed for the embed button
---

<button class="btn btn-embed hide-in-iframe" data-bs-toggle="modal" data-bs-target="#embedModal" type="button" aria-label="Open embed options for the Assignment Planner"> Embed planner </button>

<script is:inline>
  // This component doesn't need any client-side JavaScript,
  // as it just triggers a Bootstrap modal which has its own event handlers
</script>
